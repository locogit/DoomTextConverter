Converts DOOM 2024 .kfont files into pngs with correct size and offset data, simply edit the font in the .py file and add the .kfont and png to the root folder and run.

Requires the Pillow library

-pip install Pillow